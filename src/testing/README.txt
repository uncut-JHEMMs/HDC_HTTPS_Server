RUNNING THE TESTS:
    -Navigate to this directory
    -Run "source ./setEnvVals.sh" in order to set your environment variables
     or the env tests will fails (the output will complain about a null 
     value)
     -Run "make tests"
     -Run ./tests, no output means everything passed